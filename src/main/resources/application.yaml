spring:
  application:
    name: jobportal
  datasource:
      url: jdbc:mysql://localhost:3306/jobportal
      username: jobportal
      password: jobportal
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql:true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE